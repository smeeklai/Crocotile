<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testproject01"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo2"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="tuat.crocotitle.program.HomeActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="tuat.crocotitle.program.ProgramActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="tuat.crocotitle.save.SavePageActivity"
            android:label="CrocoTile File"
            android:screenOrientation="portrait" />
        <!-- Songs Activity -->
        <activity android:name="tuat.crocotitle.program.VariableActivity" />

        <!-- Videos Activity -->
        <activity android:name="tuat.crocotitle.program.ConstantActivity" />

        <!-- Photos Activity -->
        <activity android:name="tuat.crocotitle.program.StatementActivity" />
        <activity android:name="tuat.crocotitle.program.OperatorActivity" />
        <activity
            android:name="tuat.crocotitle.save.SaveInputActivity"
            android:label="Save"
            android:screenOrientation="portrait"
            android:theme="@style/myActionBar" >
        </activity>
        <activity
            android:name="tuat.crocotitle.save.UpdateActivity"
            android:label="Update" android:theme="@style/myActionBar">
        </activity>
    </application>

</manifest>